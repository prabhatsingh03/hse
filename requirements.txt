Flask>=3.0.0
Flask-Session==0.5.0
python-dotenv
mysql-connector-python
Werkzeug>=3.0.0
xhtml2pdf

# NOTE: xhtml2pdf is a pure Python library that doesn't require external system dependencies.
# It works on Windows, Linux, and macOS without additional setup.
# Install with: pip install -r requirements.txt


